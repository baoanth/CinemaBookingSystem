@model CinemaBookingSystem.ViewModels.TheatreViewModel;

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    int? currentId = Context.Session.GetInt32("currentCinemaId");
}

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Theatre/</span> Create</h4>
    <!-- Basic Layout & Basic with Icons -->
    <div class="row">
        <!-- Basic Layout -->
        <div class="col-xxl">
            <div class="card mb-4">
                <div class="card-header d-flex align-items-center justify-content-between">
                    <h5 class="mb-0">Thêm mới phòng chiếu</h5>
                    <small class="text-muted float-end text-danger">* là thông tin bắt buộc</small>
                </div>
                <div class="card-body">
                    <form method="post" asp-action="Create">
                        <div class="row mb-3">
                            <label class="col-sm-2 col-form-label" for="cinema-name">Tên phòng chiếu<span class="text-danger"> *</span></label>
                            <div class="col-sm-10">
                                <input type="text" asp-for=TheatreName class="form-control" id="cinema-name" placeholder="Phòng chiếu 1..." />
                                <span asp-validation-for="TheatreName" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="row mb-3">
                            <label class="col-sm-2 col-form-label" for="capacity">Số chỗ ngồi tối đa<span class="text-danger"> *</span></label>
                            <div class="col-sm-10">
                                <input type="text" asp-for=Capacity class="form-control" id="capacity" placeholder="300..." />
                                <span asp-validation-for="Capacity" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="row mb-3">
                            <label class="col-sm-2 col-form-label" for="cinema-select">Thuộc rạp<span class="text-danger"> *</span></label>
                            <div class="col-sm-10">
                                <select id="cinema-select" class="form-select" asp-for="CinemaId">
                                    @{
                                        foreach (var item in ViewBag.CinemaList as IEnumerable<CinemaBookingSystem.ViewModels.CinemaViewModel>)
                                        {
                                            <option value="@item.CinemaId">@item.CinemaName</option>
                                        }
                                    }
                                    
                                </select>
                                <span asp-validation-for="CinemaId" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="row justify-content-end">
                            <div class="col-sm-10">
                                <button type="submit" class="btn btn-success w-100" value="Thêm mới">Thêm mới</button> &ensp;
                            </div>
                        </div>
                    </form>
                    <div class="row justify-content-end">
                        <div class="col-sm-10">
                            <a asp-action="Index" asp-route-id="@currentId"> <input type="submit" class="btn btn-primary" value="Trở về danh sách" /></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

