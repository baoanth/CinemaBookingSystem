@model CinemaBookingSystem.ViewModels.CinemaViewModel


@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    int? currentId = Context.Session.GetInt32("currentCinemaId");
}

<div class="container-xxl flex-grow-1 container-p-y">
    <!-- Basic Layout & Basic with Icons -->
    <div class="row">
        <!-- Basic Layout -->
        <div class="col-xxl">
            <div class="card mb-4">
                <div class="card-header d-flex align-items-center justify-content-between">
                    <h5 class="mb-0">Mọi thao tác không thể hoàn tác, bạn có chắc chắn muốn xóa @Html.DisplayFor(model => model.CinemaName) ra khỏi danh sách?</h5>
                </div>
                <div class="card-body">
                    <form method="post" asp-action="Delete" asp-controller="Cinema" asp-route-id="@Model.CinemaId">
                        <div class="row justify-content-end">
                            <button type="submit" class="btn btn-danger">Xác nhận xóa</button>                    
                        </div>
                    </form>
                    <hr />
                    <a asp-action="Index" asp-controller="Cinema" asp-route-id="@currentId"> <input type="submit" class="btn btn-primary" value="Trở về danh sách" /></a>
                </div>
            </div>
        </div>
    </div>
</div>
